# Mikrofine MultiMouse Control Center

A Windows control center for multi-vendor gaming HID devices.

## Current milestone

**HID Explorer v0.1** — enumerate HID devices and inspect their descriptors/reports before implementing vendor-specific control backends.

The first implementation is intentionally read-only with respect to device reports.
